# Checks

One class per question. `Program.cs` registers them. A missing column skips the check as reduced coverage rather than a clean run. `RepeatedFailedMethodsAreFindings` asks whether one method failed more than once; `TheHandsWorkWhileThreatened` no longer treats a hunt refused as `no-reachable-firing-position` as the target chooser refusing everything.
