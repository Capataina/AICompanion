# Steering — a route, and the momentum that follows it

The navigator owns one route and one search at a time and turns them into a velocity every tick; the steering law is the pure function under it. Nothing here writes the body: the answer is a `Controls`, the velocity the motor should accelerate toward, and the motor is what applies it.

```
Steering/
├─ CLAUDE.md            this guide
├─ OrbState.cs          the body at one tick: centre, velocity, whether it is pinned
├─ Controls.cs          what the brain asks for one tick: the velocity to move toward, and whether it may burst there
├─ OrbPace.cs           the speed cap, turn authority and speed change the motor applies, and the one law that applies them
├─ Route.cs             the points from the body to the goal, the segment the body is on, validity, and line-of-sight smoothing
├─ SteerAlongRoute.cs   the steering law: a lookahead that grows with speed, an eased arrival, a cap through the next bend
├─ HoverAroundSpot.cs   what the body does wherever it has been asked to be: a slow wander around the spot, kept off walls
├─ EvadeWhileMoving.cs  safety on top of the job: the tick's controls bent away from a predicted hit, only when they would meet one
├─ Navigator.cs         plans, re-plans, steers, hovers on arrival or while a goal is out of reach, and scores how each attempt ended
└─ BehaviourCensus.cs   the whole run's accounting: places asked for, reached and abandoned, plans made and found nothing
```

## The law

The body is steered at a point ahead of its projection on the route, which is what makes it lean into a turn and cut its inside rather than track every waypoint, and a body that has drifted off the route is steered at the lookahead from its projection, which pulls it back onto the route rather than back to where it left. The lookahead is a number of ticks of the body's own speed, clamped between a floor and a ceiling, so a fast body looks further down the route and curves into a bend early while a slow one does not cut a corner it is barely moving through. The speed asked for is the cap, lowered toward the goal so the body eases in rather than overshoots — the speed from which only a share of the speed change would bring it to rest over what is left, which is what makes the slowdown gradual rather than a late stop — and lowered into a bend in proportion to how sharply the route turns at the next waypoint within braking distance.

**The motor's law has two caps, one on each axis of the heading, and `OrbPace.Step` is the only place it is written.** The change in velocity across the heading is capped by the turn authority, so the turning radius at speed is the square of the speed over it; the change along the heading is capped by the speed change, which is several times smaller, so the body gathers speed and sheds it over most of a second while still turning tightly. Below a walking pace there is no heading to split along, and the whole change takes the speed-change cap. A dodge sets `Controls.Burst` and may spend the turn authority along its heading as well, which is the one move meant to look sudden. A body that cannot turn inside a corridor's width scrapes the far wall whatever the route says; the corridor fixture's minimum-clearance row is where that showed, the turn authority is what fixed it, and splitting the speed change off it is what stopped that fix from snapping every departure to full speed in twelve ticks. NavReplay's arrival row carries the ramp as a measure and a declared band, and every headless tool integrates this same function rather than a copy.

## A route is smoothed without giving back what the search paid for

Smoothing skips waypoints while the straight segment past them keeps the body clear of walls, from the front, using the contact's own swept test. It also refuses a skip where the chord, at the point standing in for a skipped waypoint, has less clearance than that waypoint had by more than the half tile the corner lattice cannot resolve, because the search paid for the middle of a corridor through its edge cost and a chord from one wall-hugging end of a corridor to the other is clear of walls while spending its whole length nearer them. The first smoother did exactly that and undid the search, and the corridor fixture's smoothed row is what refuses it now.

## Arrival, replanning, and what the navigator reports

Arrival is a radius around the goal, and staying arrived is a wider one. Inside the arrival radius the navigator drops its route and hovers around the goal; it keeps reading arrived until the body leaves the settle radius, which is the arrival radius plus the hover's own reach plus a margin, so a drifting body does not flicker between arrived and travelling. The settle radius, not the arrival radius, is what every region the positioner admits a destination against reserves, so a spot on a region's boundary cannot be hovered around outside it. `Tools/NavReplay`'s arrival row drives that tick by tick, holds it for a hundred and twenty ticks on one search, and requires the body never to sit still for the session reader's still threshold. A goal that drifts a little re-aims the last segment; one that moves materially cancels the attempt and plans afresh. A goal whose search has not finished is flown toward along a partial route to the nearest place that search has reached, and so is a goal whose search stopped at its node limit; a goal the search has proven unreachable is hovered around a wait anchor taken where the body was when it stopped being able to go, and is published as proven unreachable, which the brain bans the spot on, rather than flown toward, because another place is the positioner's answer. Neither is answered with nothing: nothing is a body standing still. **A finished search without a route is not run again for the same goal** until the terrain it read changes, the body leaves what a proven absence explored, or a node-limited search's partial route has brought the body materially nearer; the first build ran the same proof on every tick for as long as the goal was held. **A kept conclusion answers the goal it was reached about, not the goal the navigator held a tick ago.** Each one records its own goal and is forgotten once the goal is further than the replan distance from that record, because the drift branch re-aims the held goal a little every tick: compared tick to tick a smoothly moving goal never counts as moved, and a sentinel's review of the first build found a settled-short answer and a proven absence each surviving six hundred pixels of drift, the body parked 583 px from a goal a fresh navigator reached. **Only a finished search's own choice settles the body short.** A route that ends at the free corner beside a goal the body cannot slide into is an answer about that goal; a partial route lent by a running or node-limited search is not, and the difference is carried as a flag set where the search finishes, never inferred from what else happens to be kept — inferring it from a missing node-limit answer meant a tile edit anywhere in a large search parked the body short for good. A no-progress strike does not clear either conclusion, because a strike while hovering at a kept answer is that answer being obeyed. `Tools/NavReplay/VerifyNavigatorConclusions.cs` holds each of these, and the interrupt scene in it holds the one that is easiest to lose: a node-limit answer proves nothing about its goal, so it dies with the attempt that asked, or a resumed attempt from the same place is never earned and lends no partial route. **Every hover anchor dies with the request that made it and is never somewhere the body cannot fly straight to.** The wait anchor is dropped by a new goal, an interrupt and a clear, and replaced by the body's own centre whenever the swept line to it is blocked; the first build kept it across an interrupted wait, and the next unreachable goal pulled the body 230 px back toward the old place and held it at zero against the wall between. `Tools/NavReplay`'s anchors row carries a body across a wall mid-wait to hold that.

## Hovering is a pursuit of a wandering target, and both halves of that were learned by measuring

`HoverAroundSpot` is Reynolds' wander: a target circles the spot along a flattened ellipse, and the rate it circles at is itself a small random walk with a floor, so the path keeps turning and never parks, and it reverses direction now and then so it is not an orbit. Its seed is per instance, so a headless run is reproducible. Three things about it are properties rather than tuning, and each was found by a trace rather than by reading.

- **The body pursues the target and does not seek it.** It asks for the target's own motion plus a correction toward it, capped at a drift's speed around a spot. A seek asks for a speed proportional to the gap, and a body a few pixels from its target slowed to a tenth of a pixel a tick every time the target doubled back — a body standing still with extra steps.
- **A target mirrored off a wall reverses its direction of circling with it.** Reflecting a circular path reverses its angular velocity; mirroring only the offset bounced the target straight back into the ceiling it had just been reflected off. When no mirror is clear the target falls back to the opposite point, then half the offset, then the spot itself with the circling reversed.
- **The request never presses the body into a wall it would meet; the part pointing into the wall is mirrored, not removed.** The target is clear of walls from the spot, not from the body, and a body lagging round the ellipse meets corners the target's path never touched. The contact already kills the into-wall part of a velocity and keeps the slide, but a request pointing almost straight at a step's corner leaves no slide, and on a staircase of blocks the body sat under the still threshold for fourteen ticks asking into the same step. The request is resolved against the contact first and mirrored off the net push-out — the net push rather than one tile's normal, because an inner corner is two tiles and a mirror off either face sends the body into the other. `VerifyCompanyLocalMotion`'s block-staircase row found it and holds it; its longest still run went from fourteen ticks to four.

The anchor is taken once, where the hover began, and not from the body each tick, because an anchor that moves with the body turns the drift into a slow wander away from where it arrived.

**Keeping the player company is the same wander across a moving box, and the four ways it differs are reasons, not values.** `HoverAroundSpot.Across` walks the target over the player's intent region, handed to it as a centre, a half-size and a lead rather than as the region itself, because the movement core names none of the senses it is fed by; `CoordinateMovement.Accompany` releases any held route first, since inside the region there is nowhere to go. Combined wall-and-enemy clearance still decides where the body goes — a body near the floor climbs and a body next to an enemy crosses away from it — but it is read **when a leg's destination is drawn**, not at each step of getting there; the next section carries the measurement that moved it. Among the turns a *blocked* step falls back on, clearance picks between them, and a crack is still taken when it is the only free step. The first target remains one step from the body. The target lives in the box's own frame, so the region's travel reaches the pursuit through the target's own motion and nothing adds the region's velocity a second time. **The heading is not a random walk at all any more** — that is the tour described in the next section, and the paragraph after it records why both the floor and the no-floor version of the random walk were deleted rather than retuned; the doc comment on `Across` still describes the random walk and is a stale comment rather than a second design. The pursuit is capped at the body's top speed rather than a drift's, because a body capped at a drift inside a box moving at a running player's pace falls out of the back of it and is sent home as outside. And while the box leads, its rear is closed to the target, which keeps a travelling player's companion level with him or ahead rather than trailing as a policy; the part of the box that is open moves toward what the lead asks for at the walk's own pace, because an edge that snaps carries a target standing in the part it closes along with it, and a player turning round once moved the target two hundred pixels in one tick. The box's own top is the ceiling, because the region sits mostly above the player by the owner's ruling and a second ceiling under its top would cut that part away. A refused target — one the circle cannot sweep to from the last target, or one inside the player's interference footprint — turns the walk round; where every turn is refused too, the target jumps outward along the axes to the nearest place that is allowed and swept clear from the body. That jump is what moves a body standing on the tile the player is building on or in the one-body-tall passage he is walking down, because a step is a pixel and a half and a refused tile is sixteen, so every turned step from inside the footprint is refused as well. The escape takes a place no further outside the open part, on either axis, than the body already is, rather than only places inside it, because the inside latch counts a body beyond that part as with the player: a body resting on a passage floor below the box's inset shares its overshoot with every place along the passage, and a membership test refused all of them and left the body on the tile the player was walking into. The reach flood refuses nothing here: the swept test already keeps the walk in free space connected to the body, and a place the flood holds no corner for can still be one the circle sweeps straight to.

**The accompanying target tours the box; it does not wander it, and the difference is a class rather than a tuning.** A random-walk heading *diffuses*: the time to cover a box goes as the square of its width in steps, so a turning-rate floor closes loops (six hundred idle ticks measured -0.03 to 0.81 of the room, missing the left third) and no floor wanders in place (-0.04 to 0.36, missing it too). Those are one fault seen from two sides, and no jitter value fixes either — the box is 428 px of open width against a 1.5 px/tick walk, so six hundred ticks buy nine hundred pixels of path, two crossings if spent going somewhere and nothing if spent doubling back. Coverage is not something a diffusive walk makes likely; it is something a tour makes true.

The tour draws a goal from the far *edge* of the part open to it, crosses to it, and draws again. Four properties of it are each answers to a measurement:

- **The far edge and not the far half**, because README names the destination: "easing forward to its leading edge, letting itself fall back toward the middle". Drawn from the far half, legs end around the middle and six hundred idle ticks reached 0.31 of the room against a third-line at 0.333 — five pixels short after crossing the whole box.
- **Clearance decides where a leg goes, never each step of getting there.** Scored per step, some turned step is almost always slightly clearer than straight on in open air, so the walk turned nearly every tick, dropped its leg and diffused anyway — measured *worse* than the wander at a longest still run of 12 ticks against 2, because a reversing pair of turns holds the target about one point. **A preference evaluated per step competes with a destination; the same preference evaluated when the destination is chosen serves it.**
- **While the box leads, a leg runs between the leading edge and the middle**; idle, it runs the whole box. That is README's own split — the coverage clause sits in the paragraph about idling, and "fall back toward *the middle*" is where a travelling companion goes. Drawn from the whole box while he walks, the tour led him on 57.9% of moving rows against the two thirds `VerifyTheCompanionLeadsATravellingPlayer` requires, because the body lags its target while the box advances under both.
- **A blocked step re-aims the leg along the turn rather than re-drawing it.** Re-drawing sends the next leg to whichever edge is furthest from the body, which is often back across the tiles the player is asking it to vacate: on the courtesy scene a block aimed at the companion's tile took 53 ticks to clear it against 26 for an empty hand, when a block is meant to clear it sooner.

**The "contradiction" this was blocked on for a day was a fixture missing a held key, and that is the durable finding.** Covering the box and never trailing looked jointly unsatisfiable because both walking scenes wrote the player's position and velocity without holding `controlRight`. The horizontal lead reads `PlayerSense.HeldMove`, which is the direction key alone — displacement feeds only the vertical part — so the region never led however far he travelled, its rear never closed, and half of a box centred on a man is behind him by construction. With the key held, the same tour leads him on 99.1% of moving rows with a worst-behind of 15 px. It is the same class as an NPC moved by position teaching the motion forecast nothing: **the sense reads the input, not the outcome.**

**The walk, and every hover, starts from the body whenever there is no motion to continue, and a change of request kind is what ends one.** `CoordinateMovement` releases the hover whenever a tick's request is a different kind from the tick before's. Every brain tick reaches exactly one request method, so a change of kind is exactly a tick on which the previous motion was not driven, and whatever that motion kept — the walk's place in the box, the part of the box it had open, the last target and its motion, the spot — describes somewhere the body no longer is. The walk's place once outlived a job and put the first accompanying target four hundred pixels from the body, which the body sprinted to at full speed; the same class had already bitten as a wait anchor outliving its goal and a per-goal memory surviving a drifting goal. The release is the one place the rule lives, so no piece of hover state carries a staleness test of its own; what it cannot see is a tick on which the brain did not run at all, and the walk's own jump test covers a box that moved far in the meantime. A walk that starts is started from the body itself, unclamped, with the open part widened to hold it, because clamping the body's place into the open part first put the target hundreds of pixels ahead of a body entering from behind a travelling player. `Tools/NavReplay/VerifyAccompanyingTargetIsContinuous.cs` holds all of it: after a job, a journey and a downing the first target is a step from the body, and a player turning round moves the target no faster than the walk.

## Evading is a bend, and it runs only when the job's own flight would be hit

`EvadeWhileMoving` flies the job forward through the motor's law and the contact for a short horizon against the reflex's predicted-collision predicate. If that flight stays clear it returns the job's controls untouched, which is almost every tick. If not, it scores a ring of headings at full speed plus the job's own heading and a stop, as context steering does: danger is how soon a heading is hit, every heading within a small tolerance of the least dangerous is eligible, and among those the one with the most interest wins — the job's heading carries interest, a stop carries negative interest, so the body only stops where stopping is safer than every heading that keeps it going by more than the danger tolerance.

**The keep test flies the job's own steering, not the tick's velocity held in a straight line.** The first tick of the flight applies the controls the job produced; every later tick asks a forecast what the job would ask from where the simulation has put the body. `CoordinateMovement` builds the forecast from whichever request produced the tick and the branch the navigator reports it steered by (`Navigator.LastSteering`): a route is steered by `SteerAlongRoute` with the forecast's own copy of the segment index, a direct line is the same eased ask toward the goal, a hover is `HoverAroundSpot.Pursue` toward the target it last chose, held still for the horizon because the hover's speed keeps that target's path short, and keeping the player company is `HoverAroundSpot.PursueAcross` toward its walking target carried on by the target's last motion each simulated tick, because that target moves at the region's pace and held still it forecasts a body slowing onto a point the walk has already left. Accompanying declares itself a producer like every other request; before it did, the keep test flew whichever request had produced an earlier tick. None of it writes the navigator, the route or the hover. A tick with no forecastable producer holds its controls, which is the old reading. The straight-line reading is the dead end: in the third orb play a route running down along the top of a pool read as flying into the pool, the job was refused with no hit anywhere, and the choice that followed pinned the body to a slope for 2,491 ticks. The general form: a safety test run against a flight the body is not going to fly refuses the job for hazards the job avoids.

**A heading that takes the body nowhere is scored as the stop.** The contact holds a body out of a wall, so a heading straight into one reads safe and dry for the whole horizon while the body does not move, and interest then prefers it to every heading that moves — the other half of that pin, a body asking for full speed into a slope every tick. A candidate whose simulated body ends the horizon less than one body radius from where it began leaves the running and the stop stands for it, so it can never beat a heading that moves, and a tie is the stop's zero request rather than a shove into the wall. The radius is the floor because a smaller displacement cannot carry the body clear of anything, while a free burst from rest covers roughly ten radii over the horizon.

**Liquid plays no part in the bend.** Every liquid is air to the body since the owner's ruling of 15 September 2026, so a heading through a pool is scored exactly as one through air, and a body squeezed between two shots above lava dodges into the lava; NavReplay's evade row requires it to. Until then water and lava the body was not immune to were a mask over the candidates rather than a danger traded against a hit.

**A bend that only postpones a hit gives the body back to the job.** The layer sees one lookahead, and a body fleeing ahead of a threat that keeps coming reads clear on every tick of it however far it flees, so on the tick it decides, a corridor's retreat and a turn from one passing shot look the same and no per-tick rule can tell them apart. `CoordinateMovement` keeps the one fact that does: how far the bends have carried the body back along the way the job asks, counted from the first bent tick, paid back by every tick the body makes way again, and not forgotten on a tick whose flight happens to read clear. Once that retreat exceeds a lookahead of full-speed flight the layer is postponing rather than avoiding, so the job's controls go out on every tick that would have bent, recorded as `spent`, until the lost way is made up; a tick with no predicate or a different kind of request clears it. A body whose dodges carry it back and forth about a place it holds pays each retreat back on the return and is not spent; one whose dodges keep carrying it the same way against the job's ask for a lookahead of flight is spent, and the job then brings it back. So the rule does not keep a hovering body in the place it arrived at under a long stream it can only dodge one way — that is still open. Three alternatives lost. A cap on how many ticks a run of bends may last got the corridor home but gave hits back everywhere the bend was genuinely avoiding them — a body holding a spot against a wall went from none to a hundred — because duration is not the quantity; distance given up is. Forgetting the retreat on any clear tick never summed past one dodge, because a body fleeing at full speed ahead of a slower shot forecasts the job fleeing too, reads clear, and resets. Refusing headings that reverse the job's way outright broke the turn from a single shot and made the stop win, which takes more hits than flying the job. Reading further ahead would not help either: the production predicate answers nothing past its lookahead, and a body out-running a threat stays clear at any horizon until it meets a wall. `Tools/NavReplay/VerifyEvadeUnderSustainedFire.cs` holds it.

Every tick's verdict is published as `CoordinateMovement.LastEvade`: whether the layer was off, kept the job, bent it for a hit, or flew the job because the bends were spent, the horizon tick at which the job's own flight met it, the candidate chosen, and how many candidates were refused for going nowhere and for danger. The recorder writes it at the end of the row. While water and lava still hurt, the contact the simulation runs pushed out of solid tiles only, so the first build scored against the contact alone and flew a body squeezed between two shots twenty-five pixels into lava; the second counted liquid as one more "unsafe at tick N", which held at the player's pace and failed at three times it, where the fleeing body outran one shot to the world's edge, found the lava a tick later than the shot and chose the lava. The general form outlived the hazard: two outcomes that are not the same size of mistake cannot share one scale, because the scale then chooses between them by timing alone. The answer bursts, so it turns away at the turn authority rather than easing. It replaced a reflex that took the body and a spacing search that held it, both of which suspended whatever the body was doing; nothing here touches the route, the activity or the hands.

A route is re-planned when the world is edited under it (the route keeps the box it was planned over and asks the edit record), when the segment ahead is no longer clear for the body, or when net displacement over a window stays under a floor — two such windows on one goal are two strikes, and the brain hands the goal back to the positioner, which bans the spot for a while. The status names the difference between a route being flown, a search still pending with the body following the route it had or steering straight where the line is clear, a proven absence, and arrival; every attempt ends completed, cancelled, pre-empted or failed, and the census counts places asked for against places reached so the record reads "asked for forty places, reached eleven" rather than a minute of following as thousands of requests. What planning cost is accumulated over every plan call and handed over by `TakePlanMs`, which resets it, so the one reader that asks once a row gets that row's own cost and a row with no plan reads zero; a last-plan figure left standing until the next plan made a sum over rows count one plan once per row.

## Traps

- `OrbPace` is written by the motor every tick from the live player and defaults to the fallback pace in `Selection/BehaviourWeights.cs`; a headless tool that never writes it runs the body a plain player would produce, and a fixture that changes the pace mid-run changes the braking distance the law reads next tick.
- The lookahead is the point drawn gold on the overlay and written to the record, and it is a steering target and not a waypoint: a body steering at it is often not heading at any point on the route.
- An evade that fires every tick a threat is merely near is a body that never flies its job. The predicate is a predicted collision within the horizon, never proximity, and the job's controls are tested before any heading is scored, so a threat the job's own flight already avoids costs nothing.
- The accompanying walk's parts interact, so a part is judged dead only against the final combination. The outward escape was once switched off with every accompanying row green and taken out, but that run still carried a target-motion rule that kept a refused body drifting; with that rule gone as well, a body under a block the player aimed at stayed on the tile for every tick of the courtesy scene and a passage walker met twelve stationary ticks. Mutate one part at a time against the tree that will ship.
- Two caps, and they are not interchangeable. The spot hover's is a drift's speed, because a body held at a tool stand or a firing spot should float about it; the accompanying pursuit's is the body's top speed, because the box it moves about travels with the player. Capping the accompanying pursuit at the hover's speed looks like a harmless unification and is the defect: at a running player's pace the body falls out of the back of the region and is sent home as outside, over and over.
- **A body flying *away* from its player reads as a recovery defect and usually is not one.** `SeekDestination` aims the navigator straight at a request's anchor whenever the positioner admits no candidate, so a wrong anchor is invisible on every ordinary tick and is the whole answer on the tick it is read; on the locked-door scene of 21 September the body flew from column 38 to column 8 — thirty tiles away from a player at column 80 — under `act=keep-company kind=WithPlayer src=seeking-destination`, until the separation finally admitted recovery flight at tick 192 and recovery, which ignores terrain by design, carried it through a sealed wall. `../Position/CLAUDE.md` owns the cause and the rule it produced. What belongs here is what reading it costs: **a run that has entered recovery flight is no longer measuring anything the navigator did**, so a minimum taken over the whole run cannot tell a body that travelled and then tripped the recovery radius from one that tripped it first and travelled during recovery, and those are opposite defects. Split the travel by phase. And the control arm caught this rather than the assertion, for the second time in that one fixture — the first was a wall built only thirty rows above the floor that a flying body went over — so two unrelated causes produced one byte-identical signature and neither was found by the thing being asserted. **The class has its own row since 22 September 2026**, `Tools/EngineReplay/Movement/VerifyASealedCompanionDoesNotFlyAway.cs`: a stone wall to the world margin, the player forty-two tiles east of it, nine hundred ticks, and the body may not travel west of where it started or widen the gap to him. It is deliberately not in the door file, because a door has nothing to do with it and a door scene is merely one way to seal a companion off; and the door fixture's premise reports this class only by *declining* to report on doors, which a body that never moved would satisfy just as well. Its before-recovery figures are asserted ahead of its own recovery premise, because the defect ends in recovery and a premise failure would otherwise abort the row before the assertion it exists for ever ran. Planting `Vector2.Zero` back as the companionship anchor reddens it at column 8.9 under `keep-company/WithPlayer/seeking-destination`, before any recovery.

What the row does **not** assert, and the reason is a design question rather than an omission: that the body closes on the barrier. Today a sealed companion holds where it lost the way — twelve tiles short of the wall in that scene — because no corner inside a region lying wholly on the player's side is admissible, so the follow intent reaches `SeekDestination`, the navigator proves the anchor unreachable and hovers at a wait anchor on the body. Making it go as near to him as free space allows is the walker's deleted partial-progress destination wearing new mechanics — a place that can be arrived at without achieving anything — so it needs the owner's ruling, and the honest place to build it would be the navigator's wait-anchor choice (the nearest *reached* node to the goal rather than where the body stood) rather than a destination the positioner invents.

## Current state — 2026-09-21

**The accompanying walk became a tour on 21 September and the coverage row is green for the first time.** `9afb2ed` replaced the random heading and `698608d` landed the tour with its four properties; on the whole-suite run at `2cbd503` every accompanying case passes — never still for ten ticks, inside the region once there, through both outer thirds and both halves of the box, and leading a travelling player on 99.1% of moving rows with a worst-behind of 15 px. Nothing else in this folder changed that day: the steering law, the route, the evade layer and the navigator's conclusions are as the orb rebuild of 15 September left them.

**The finding that cost the day is not about steering at all.** Covering the box and never trailing read as jointly unsatisfiable for a day, and the cause was a fixture writing the player's position and velocity without holding `controlRight`; the horizontal lead reads `PlayerSense.HeldMove`, the direction key alone, so the region never led however far he travelled and half a box centred on a man is behind him by construction. **The sense reads the input, not the outcome** — the same class as an NPC moved by position teaching the motion forecast nothing.

## Planned work

- **The doc comment on `HoverAroundSpot.Across` still describes the random-walk heading the tour replaced**, and it is the summary a reader meets first. Recorded rather than edited: a droid writes documentation and never code.
- The evade layer's spent rule still has no bound on how late a job's arrival can be under sustained fire, and it does not keep a hovering body in the place it arrived at under a long stream it can only dodge one way. Both are named in the evade section above as open rather than solved.
